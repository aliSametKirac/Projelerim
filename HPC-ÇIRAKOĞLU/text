npm config set legacy-peer-deps true

/* customClustvis */

sudo docker run -d \
  --name customClustvis \
  -p 3840:3838 \
  -v $(pwd)/customClustvis/ClustVis-master/:/srv/shiny-server/:ro \
  taunometsalu/clustvis

sudo docker run -d \
  --name gene.iobio
  -p 4028:4026 \
  -v $(pwd)/gene.iobio \ 
  gene.iobio